# HearClear 待迭代事项

> 更新时间：2026-09-24  
> 本文只记录已确认但本轮不实现的事项；候选方案需经过真实网站和目标设备验证后再进入实现。

## 1. 超前识别与翻译

**状态：未实现。**

当前无字幕流程通过 `tabCapture` 获取正在播放的标签页音频，每累计约 5 秒再识别并翻译，因此只能近实时处理已经播放的内容，不能提前准备后续字幕。

后续迭代目标：

- 对能够安全取得未来字幕 cue 或未来媒体音频的页面，提前识别／翻译播放头之后的小段内容。
- 不首启处理整部影片；超前窗口必须有上限，并展示当前超前量或落后量。
- seek、切换视频或媒体源变化后，废弃旧位置的任务和结果，不把旧字幕显示到新位置。
- 无法读取未来媒体数据时继续使用当前 `tabCapture` 近实时路径，并明确展示当前模式。
- 不绕过 DRM、鉴权、跨域或平台访问控制，不默认上传原始音频。

候选方向仅用于后续验证：已有未来字幕 cue 时优先批量翻译；直接媒体 URL 可评估受权限约束的 Range 获取；`blob:`／MSE、加密媒体及站点私有播放器需分别验证，不预设通用方案。

验收标准：

- 在明确列出的支持站点上，字幕可稳定领先播放头，且超前量有可观测指标。
- 连续播放不积压；暂停、seek、切换视频后没有旧字幕回流。
- 不支持超前处理的网站自动退回近实时模式，识别与翻译仍可继续工作。

## 2. 字幕颜色可调

**状态：未实现。** 当前仅支持字号和背景透明度等样式，原文与译文颜色固定。

后续迭代目标：

- 分别设置原文颜色和译文颜色。
- 提供恢复默认值；设置即时预览并持久化到本机。
- 保证基本可读性，不允许透明文本或与背景无法区分的无效组合。
- 视频字幕层与侧栏字幕面板使用相同的颜色配置。

验收标准：刷新页面、切换视频和重启扩展后设置仍生效；普通页面与全屏显示一致。

## 3. 字幕位置拖动

**状态：未实现。** 当前字幕使用固定的底部位置，设置中的位置值尚不能由用户拖动调整。

后续迭代目标：

- 用户可以在视频区域内拖动字幕，且拖动不触发播放器点击或 seek。
- 位置按视频区域的相对坐标保存，以适配窗口缩放、页面滚动和全屏切换。
- 限制字幕留在可见区域，并提供恢复默认位置。
- 页面替换播放器或切换视频后重新绑定位置，不保留失效的像素坐标。

验收标准：YouTube 与至少一个通用 HTML5 视频页面中，普通／全屏模式下均可拖动，窗口缩放后位置仍合理且不遮挡播放器关键控制。

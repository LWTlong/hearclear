# AI 协作约定

## 硬性规则

- 改动结束后，用项目已配置的命令对本次涉及的文件做一次类型检查和 lint 检查；只修本次改动引入的问题，历史遗留问题不动，也不做无关重构。
- 禁止修改 node_modules/、构建产物目录（dist/、build/、.next/ 等）。
- 不主动提交涉及密钥、账号、token 等敏感信息的改动；用户明确要求提交时先提示风险，确认后再执行。新代码涉及密钥用环境变量，不硬编码。
- 改动引发明显问题时，优先回滚或最小修复，不在错误基础上继续堆叠。
- 不主动执行 git commit；代码生成后功能是否验证通过不确定，提交交由用户自行判断。需要时只给 commit message，不代提交。
- 禁止随意生成文档文件；只有用户明确要求写文档，或询问是否需要文档并得到确认后才生成。
- 收到需求或问题修复请求时，先给方案再动手：需求先给技术方案，修复先查根因再给修复方案连同根因一起说明；等用户确认后才改代码，不要先改再问。方案必须基于项目真实代码和可行性、真实可落地，讨论中禁止臆想和迎合。
- 新增依赖包或新能力（测试框架、工具库等）必须先询问用户并得到确认，不擅自引入。

## 一、改业务代码前

1. 查下面的路由表，找到对应业务单元，**读它的知识文件**再动手。里面是代码里读不出来的东西：为什么这样规定、踩过什么坑。
2. 需要影响范围（谁引用了它、它依赖哪些高危共享文件），跑 `node scripts/knowledge-build.mjs`，看 `.knowledge/view.md`。**不要靠猜，也不要相信任何文件里写死的影响范围**——那种一定过期。
3. 见到 `// KEEP:` 注释，不要动那段代码。

## 二、觉得某段代码"不合理"时，先查为什么

有些代码是临时方案或特殊原因才这么写的，改掉就出问题。判定不合理之前按顺序查：

1. 有 `// KEEP:` → 原因就在注释里
2. 没注释 → `git log -L <起>,<止>:<文件>`。如果那几行最后一次改动来自 `fix` 提交，提交信息通常就是原因
3. 两条都查不到 → **问，别改**

## 三、写下"不合理但必须"的代码时

同一次编辑里补一行注释，不要等以后：

```ts
// KEEP: 初始化时故意传空字符串，不把 props.modelValue 传给 Jodit
//       —— iframe 初始化竞态会导致重复渲染（7747f253）
```

只用 `KEEP:` 这一个标记。仓库现有 0 个 `FIXME` / `HACK` / `XXX`，别引入第二套词汇。


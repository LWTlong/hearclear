# HearClear · 听懂 — 首版开发需求与交付说明

版本：v0.1 需求稿  
整理日期：2026-09-24  
面向执行者：本地 Claude / Claude Code  
目标：交付一个可在真实网页视频上使用的 Chrome 扩展，而不只是界面原型。

> 本文区分「用户明确需求」「建议的首版取舍」「需要实测的技术假设」。建议不能写成已经验证的能力。遇到限制要展示事实，不要为了满足描述而宣称“所有网站都能用”“零延迟”“任意 API Key 都支持”。

## 1. 项目背景与产品目标

项目名称：**HearClear**；中文名：**听懂**。

一句话介绍：为网页视频和音频提供实时中文字幕或双语字幕，优先翻译现成字幕，读取不到可用字幕时再识别声音。

用户已尝试多个浏览器扩展和 Windows 软件，遇到强制订阅、只能接官方 Gemini Key、要求特殊 Realtime API、没有可安装版本或配置后持续报错等问题。因此决定先开发自用版本，优先解决实际观看体验。

理想流程：

1. 用户打开正在播放视频或音频的网页，主动启动 HearClear。
2. 检测可访问的原文字幕。
3. 有可用字幕：直接翻译字幕，依据播放器时间显示。
4. 没有字幕，或字幕无法读取：在获得采集授权后，识别当前标签页声音。
5. 把识别得到的文字交给用户自己的翻译 API。
6. 在视频附近显示中文或“原文 + 中文”，允许暂停、拖动进度、切换视频。

总体优先级：**通用性 > 已有字幕优先 > 无字幕识别 > BYOK > 免费/非订阅 > 隐私 > 自动识别语种**。首版先保证完整流程和稳定性，再扩大站点覆盖。

## 2. 已知用户环境与明确约束

- 主要验收环境：Windows 10 PC + Chrome。架构需要兼容 macOS Chrome，不能把 Windows 独有识别能力做成必需依赖。CPU、显卡、内存尚未提供，不预设有 NVIDIA GPU。
- 产品形态已经选择 Chrome 扩展；第一版不同时开发独立桌面应用。
- 内容不局限 YouTube，包含一般网页视频、直播、课程、网页音频。英语、日语、韩语是优先源语言，简体中文是默认目标语言。
- 用户可以接受有字幕时仍用音频识别，但产品默认仍应优先使用可读取的字幕。
- 用户已有自己部署的 **Sub2API** 与 API Key。现有地址为 `https://speed.bobiking.com`；它是用户配置示例，不要写死到产品或默认向它发送任何数据。
- 当前只把该服务作为候选文字翻译接口。之前试用的插件要求 `/realtime/translations` 专用接口，不能据此假设 Sub2API Key 能用于语音识别或实时语音翻译。
- 不希望为了 AI 翻译额度再购买 SaaS 订阅；HearClear 首版不设登录、套餐、会员或额度售卖。
- 希望安装扩展后即可使用，允许首次下载模型；尽量不要求安装 Python、Docker、本地服务或虚拟声卡。
- 默认优先本地识别，音频不上传；只把字幕文字发送到用户配置的翻译服务。语音识别与文字翻译必须独立配置。
- 播放体验很重要，拖动进度条是高频操作。不能在跳转后继续显示旧位置的字幕。

## 3. 首版范围与默认取舍

以下是为了尽快交付提出的首版方案，允许基于实测调整实现，但不得悄悄删除用户的核心需求。

### 3.1 首版必须完成

| 能力 | 首版要求 |
| --- | --- |
| 安装 | 提供可构建、可“加载已解压扩展程序”的完整目录，不要求日常使用时启动开发服务器 |
| 网页覆盖 | 通用 HTML5 video/audio；对特殊播放器给出明确支持状态 |
| 任务数量 | 同一时间一个标签页、一个选中的播放器 |
| 已有字幕 | 至少支持原生 TextTrack / WebVTT；字幕获取采用可扩展适配器 |
| 无字幕 | 必须有真实音频采集、真实 ASR、真实翻译和字幕显示的完整路径 |
| 翻译 | 普通 OpenAI-compatible 文字接口，支持自定义地址、Key、模型 |
| 语言 | 中译目标；英、日、韩分别验证，允许用户手动指定源语言 |
| 播放控制 | 播放、暂停、拖动进度、切换视频、进入/退出全屏 |
| 字幕 | 原文、译文、双语切换；字号、背景透明度、位置调整 |
| 诊断 | 模型、音频采集、ASR、翻译、站点权限分别显示状态和错误 |
| 隐私 | Key 默认仅保存在扩展本机存储；默认不保存原始音频 |

### 3.2 首版暂不承担

- 系统级音频、原生会议客户端、桌面悬浮窗口、Safari/Firefox、移动端。
- 配音/TTS、语音克隆、说话人识别、会议纪要、词典、背单词。
- 账户系统、付费体系、云同步、团队管理、商店上架。
- 直接播放本地文件、长视频完整下载、整片预转录和预翻译。
- 保证所有 DRM、封闭播放器、跨域 iframe 均可读取字幕或捕获音频。
- 用 OCR 读取烧录在画面里的字幕。

### 3.3 扩展方向，首版留接口即可

- YouTube 等站点的字幕适配器；可在首版核心流程通过后加入第一个站点适配器。
- 用户显式选择的云 ASR、本地辅助服务。
- 更多目标语言、字幕导出、历史记录、跨会话字幕缓存。
- 可控的预处理/缓冲观看模式，用于降低字幕相对画面的滞后。

## 4. 必须先验证的可行性问题

开发从小型、可运行的技术验证开始，验证通过后直接继续实现，不要先花大量时间制作完整 UI。验证不通过时，交付已做的验证结果和具体阻塞点，不要用 Mock 替代真实能力并宣布完成。

### 4.1 浏览器内 ASR 是候选方案，不是已经确定的结论

优先评估浏览器内的多语种 Whisper，例如 multilingual tiny/base 档，通过实际可用的 WASM/WebGPU 推理运行时执行。不要使用只能识别英语的 `.en` 模型来声称支持日语、韩语。

必须在扩展实际运行环境验证：

- 音频采集、重采样、语音分段、模型推理能否完整跑通。
- 模型下载地址可访问，模型版本与运行时兼容。
- 当前设备是否支持所需 WebGPU 能力；不可用时是否有可工作的 WASM/CPU 路径。
- 英、日、韩测试片段的识别质量、速度、内存、冷启动时间。
- 连续输入时处理是否跟得上音频，是否会逐渐积压。

如果浏览器内 ASR 无法达到可观看水平，说明瓶颈和测量结果，列出最小替代方案及额外要求。**不能静默把本地识别改为上传音频，也不能默认要求用户开通新的云服务。** 仅提供“接外部 ASR 的空接口”不算完成无字幕功能。

### 4.2 字幕可读性和站点覆盖

先证明原生 TextTrack / VTT 路径可用，再增加特定站点适配。原生字幕轨、播放器私有字幕、画面烧录字幕是不同情况。

“没检测到字幕”不等于“视频绝对没有字幕”。检测结果至少区分：可用、未发现、读取受限、加载中。

### 4.3 Sub2API 翻译接口

只以用户实际配置的文字接口和模型测试结果为准。不要要求 `gpt-realtime-translate`，不要把文字 Key 填进 ASR 配置，也不要为了适配扩展去修改用户现有网关。

开发时使用本地录入的 Key；文档、代码、截图、日志和提交中不得出现真实 Key。

## 5. 用户操作流程

### 5.1 首次使用

1. 加载/安装扩展，打开设置页面。
2. 配置翻译服务：接口地址、API Key、模型，点击“测试翻译”。
3. 选择识别方案；本地模式展示模型大小、下载来源、存储位置说明和下载按钮。
4. 下载完成后显示“识别已就绪”；失败时可以重试或更换受支持模型。
5. 回到视频网页，点击“开始翻译”。

只有用户主动启动后，才允许开始采集。自动降级到 ASR 的含义是在这次授权的会话中自动切换，不能绕过浏览器的用户手势或权限要求。需要额外点击授权时，明确引导。

### 5.2 日常使用

- 打开视频，点击开始；默认模式为“自动：字幕优先”。
- 显示当前模式：“翻译现有字幕”或“正在识别声音”。
- 支持手动切换为“只使用现有字幕”或“强制识别声音”。
- 用户选定目标语言、字幕显示方式后，后续沿用设置。
- 暂停、快进、后退、切换视频不要求重新填写 API 配置。
- 点击停止后立即停止新增识别/翻译请求，释放采集资源。

多个播放器时，优先识别正在播放且可见的主要播放器，存在歧义时让用户选择。标签页音频采集可能包含该页其他声音，不能承诺天然隔离到某一个 video；检测到多音源时给出可理解的提示。

## 6. 已有字幕流程

### 6.1 获取与选择

- 将所有字幕源归一为带 `startTime`、`endTime`、原文和来源标识的 cue。
- 支持字幕轨加载较晚、轨道切换、播放器动态创建和销毁。
- 允许用户选择字幕轨。已有可用中文字幕时直接显示，避免重复翻译。
- 没有当前 cue 可能只是静音或字幕间隙，不能因此立刻切换 ASR。
- 对私有播放器采用适配器，通用流程不能堆满某个网站的 DOM 选择器。
- 跨域/iframe 无法读取时说明限制，不绕过平台访问控制。

### 6.2 翻译与显示

- 能取得未来 cue 时，优先翻译当前位置附近及之后的小段字幕；不要首启就翻译整部影片。
- 合并短句、批量翻译时保留 cue ID 与时间关系，校验返回条数/标识，防止字幕错位。
- 请求可以异步返回，但显示必须跟随播放器 `currentTime`。
- 译文尚未完成时先显示原文和轻量状态；不得阻塞视频或清空整块界面。
- 已经远离当前播放位置的迟到译文不得作为“当前字幕”突然弹出。
- 翻译失败可以保留原文，提供重试；不得无限重试收费接口。

翻译缓存应包含视频/媒体标识、轨道/语言、原文、目标语言、模型及相关配置版本，不能只用一句文本或页面 URL 作为跨视频命中依据。首版会话内缓存即可；持久化缓存属于后续功能。

## 7. 无字幕音频识别流程

### 7.1 音频采集

- 优先采集当前授权标签页的声音，不以麦克风外放回录作为默认方案。
- 不依赖耳机或扬声器类型，不引入 Windows 专用音频回环服务作为必要条件。
- 采集过程中原视频声音应正常播放，无意外静音、双重播放或回声；停止时正确恢复和清理。
- 不把浏览器内部页面、商店页、受保护页面等不支持场景当作普通采集错误无限重试。
- 页面跳转、播放器更换、视频源变化、标签页关闭时及时清理或重新建立会话。

### 7.2 分段与识别

- 实现重采样、语音活动检测或等效静音处理、带边界的音频缓冲、识别任务队列。
- 避免静音、音乐和噪声反复生成无意义字幕；不得宣称彻底消除幻觉。
- 保留适度上下文以减少句子切断，同时去重重叠片段。
- 将不稳定的临时识别与已确认文本区分；避免每个临时 token 都触发一次付费翻译。
- 支持手动指定英语、日语、韩语。自动语言识别可作为增强功能，检测不确定时允许固定语种。
- 音频队列必须有上限。跟不上时显示“识别落后”和可执行选项，不能隐藏问题或无限积压。
- 如采取丢弃过期片段来追赶播放位置，必须提示发生了跳过，不能表现为完整无遗漏。

### 7.3 本地模型管理

- 模型保存在该扩展控制的浏览器存储/缓存中；具体采用 IndexedDB、Cache Storage 或其他方案由验证结果决定并记录。
- 展示下载进度、实际下载量、错误和重试；记录已安装版本，避免每次重下。
- 支持删除模型和查看占用；明确浏览器清理数据、卸载扩展可能导致模型重新下载。
- 模型权重可按需下载；执行用的 JS/WASM 运行时代码随扩展打包，避免依赖远程执行脚本。
- 验证存储配额、下载中断、版本不匹配、缺失文件等情况。
- 不预设模型只需几十 MB；显示最终采用版本的真实下载量和运行内存测量。

## 8. 播放同步、暂停、快进与倍速

这一部分是核心验收项，不是后续优化。

### 8.1 已有字幕的同步

- 使用媒体时间轴决定字幕出现和消失，不按“请求返回后显示几秒”来替代。
- 暂停时保留当前 cue；恢复后继续跟随播放时间。
- 快进/后退后立即重新选择当前 cue，优先请求新位置译文。
- 旧请求可以安全写入正确的视频缓存，但必须禁止覆盖当前屏幕状态。

### 8.2 ASR 的同步边界

实时听声音必须先接收到音频才能识别，因此一定存在处理延迟。**录入时间、识别完成时间、字幕显示时间不是同一个时间。** 禁止承诺仅靠时间戳就能消除滞后。

- 已能访问播放器时，为音频片段记录对应的媒体时间范围，不能用识别完成时的 `currentTime` 当作原句开始时间。
- 时间映射要考虑播放、暂停、seek、倍速变化；不得用一条固定起点公式贯穿所有状态。
- 直播/无法取得媒体时间的页面使用明确标记的实时采集时间线，不伪装成精确的视频时间轴。
- ASR 字幕采用近实时显示，限制最大陈旧程度；落后明显时提示用户，提供清空积压/降低模型负载等选项。
- 首版不默认自动暂停用户的视频，也不擅自改变视频速度或延迟画面。

### 8.3 拖动进度和视频切换

建议使用 `sessionId` + `generation` 或等效机制隔离异步结果。

发生 `seeking`、媒体切换或源变化时：

1. 立即清除屏幕上的旧位置 ASR 字幕。
2. 标记旧识别/翻译任务失效；可以取消的请求立即取消。
3. 清空旧音频缓冲、VAD 状态、待翻译队列，重建媒体时间映射。
4. 旧任务即使稍后返回，也不能显示到新位置。
5. seek 完成后，从新位置重新采集，或复用同一媒体已验证有效的字幕缓存。

音频采集过程中产生的边界残留也要处理，不能只清 UI 却继续识别旧缓冲。

如果站点无法暴露播放器状态，应明确说明自动 seek 同步不可用，提供“清空并重新开始”入口，不把此场景计入精确同步支持范围。

### 8.4 暂停和倍速

- 暂停时停止把新的非播放音频持续提交为该视频内容；可收尾处理暂停前最后一句，但不能反复提交相同片段。
- 恢复时排除暂停区间，避免时间轴漂移。
- 已有字幕跟随媒体时间，因此应自然适配倍速。
- ASR 在倍速变化后重建映射，并评估识别质量/吞吐下降。1.5×、2× 超出设备能力时应明确提示，不能让队列越来越长。

## 9. 翻译 API 与配置

### 9.1 首版接口

首版实现 **OpenAI-compatible Chat Completions 文字翻译**。原生 Gemini、Claude、云 ASR 和专用 Realtime 协议留扩展点，不要求首版全部完成。

配置项：服务名称、Base URL 或完整请求地址、API Key、模型 ID、请求超时、有限并发。设置页说明各字段含义并展示最终调用地址。

URL 处理必须明确：

- 用户的 Base URL 示例：`https://speed.bobiking.com/v1`。
- 其对应的候选完整地址：`https://speed.bobiking.com/v1/chat/completions`。
- 根地址、含 `/v1` 的地址、完整 endpoint 不得被重复拼接成 `/v1/v1/...` 或 `/chat/completions/chat/completions`。
- 若提供完整 endpoint 模式，应按用户明确选择的模式处理；对自定义路径不能猜测后随意改写。

这些只是用户服务的配置示例，是否可用需要带用户自己的 Key 和真实可用模型进行本机测试。模型 ID 不写死、不默认使用语音模型。

### 9.2 请求与验证

- “测试翻译”实际发送一条短文字翻译请求，而不只是检查 Key 是否非空或列模型成功。
- `/models` 发现是可选便利功能；列模型失败不应直接判定聊天接口不可用。
- 控制字幕上下文长度、请求频率、并发和缓存，避免按每个识别碎片收费。
- 翻译提示要求仅返回译文，字幕内容作为待翻译数据处理，不能把其中的指令当作系统指令执行。
- 校验响应结构、空结果、缺失 cue、异常文本；错误结果不污染缓存。
- 首版优先支持常见非流式响应即可；是否加入流式显示以复杂度和实际收益决定。
- 认证失败、无权限、模型不存在、余额/限流、服务器错误、超时、网络/证书、扩展未获目标域权限分别提示。
- 错误信息应包含出错阶段、可获取的 HTTP 状态、脱敏的服务端信息和建议操作；避免只显示“连接失败”。

## 10. 界面与状态

### 10.1 Popup

展示当前页面/播放器、开始/停止、源语言、目标语言、工作模式、原文/译文/双语开关、设置入口。关闭 Popup 不应终止正在工作的会话。

### 10.2 字幕层

- 默认在视频底部附近显示双语字幕；中文清晰、背景可读，样式尽量不受网页 CSS 污染。
- 可调整字号、背景透明度、位置；避免遮挡播放器主要控制按钮。
- 自动换行，限制同时显示的行数，避免长段文字铺满画面。
- 常规页面和可控的全屏播放器都要验证；无法在特定全屏实现覆盖时明确提示。
- 悬浮层以字幕阅读为主，API 地址、模型下载细节等放在设置和诊断区。

### 10.3 设置和状态

设置页包含翻译配置、本地模型管理、显示偏好、隐私说明、缓存清理和诊断。

至少区分：未启动、检测字幕、等待授权、下载模型、加载模型、翻译现有字幕、识别声音、暂停、处理落后、出错、已停止。按实际流程设计状态机，避免散落布尔值导致 UI 和后台状态不一致。

## 11. 架构建议与安全边界

这些是实现建议；Claude 应核对当前依赖与浏览器版本，再确定方案。

- Manifest V3 + TypeScript。设置/Popup 优先 Vue 3 + Vite，使用项目中已有且合适的组件，避免为简单设置页引入过重依赖。
- Content Script：发现播放器、字幕读取、时间轴事件、字幕渲染。
- Service Worker：会话管理、权限与配置、可信请求调度；不要假设它永久常驻。
- Offscreen Document：音频流和必要的后台页面能力；通过消息与扩展其他部分协作。
- Worker/合适执行环境：模型推理、音频处理，避免阻塞页面和 Popup。
- 抽象播放器适配器、字幕源、ASR Provider、Translation Provider、字幕调度器，保持最小清晰边界，不建立过度通用的平台框架。
- 明确所需 Chrome 最低版本；若采用 Service Worker 获取 stream ID 后交给 Offscreen 的路径，要考虑 Chrome 116 起的相关行为。不要一边声明兼容较旧版本，一边无检查使用最新 API。
- 扩展跨域请求在可信扩展上下文执行，按实际需求申请目标域权限。不能通过关闭浏览器安全机制来“修好”请求。
- Key 默认只放扩展可信本地存储，不放网页 localStorage，不传入页面 DOM/主世界，不使用 `storage.sync` 自动同步。
- 本地保存不等于加密保险箱；README 不得宣传为绝对安全或不可读取。
- 默认关闭音频持久化和正文日志，不引入分析埋点、远程音频中转或秘密兜底服务。
- 若未来启用云 ASR，应明确显示音频将发往哪个提供商，必须由用户主动选择。
- 启用本地识别仍可能需要首次联网下载模型；不能把这一点描述为首次安装即可完全离线。
- 使用开源模型和运行时时记录其版本与许可，确保有权按所选方式分发。

## 12. 性能和体验验收

以下时间是首版的建议体验目标，**不是已经得到验证的性能承诺**。Claude 应保留测量方法，列明硬件、浏览器、模型、网络与测试素材；达不到时如实报告。

| 项目 | 目标或判定方法 |
| --- | --- |
| 有字幕显示 | 以原字幕时间为准；已经预译的 cue 不应因请求到达顺序错位 |
| ASR 延迟 | 以一句话结束到其可读中文出现计算；争取常见清晰音频约 1–3 秒，记录 P50/P95；频繁超过 5 秒视为观看体验未达标 |
| 冷启动 | 单独记录模型下载、加载和首句时间，不混入稳态延迟，也不能隐瞒 |
| 持续吞吐 | 连续播放至少 15 分钟，队列和落后时长不能持续增长 |
| seek 清理 | 建议收到 seek 事件后 300ms 内清掉旧字幕；旧任务回来后不得重新出现 |
| 停止 | UI 立即响应，尽快释放采集/任务资源；已经发出的服务端请求可能无法撤销或免除费用，不做此承诺 |
| 资源 | 记录峰值内存、CPU/GPU 行为；不能导致播放器明显持续掉帧或网页失去响应 |

自动化测试优先覆盖会产生真实错误的逻辑：URL 拼接、cue 时间选择、请求乱序、seek 后过期结果隔离、重复启动/停止、缓存隔离。不要靠大量只验证实现细节的测试替代真实视频试用。

### 必测场景

| 场景 | 通过条件 |
| --- | --- |
| 普通 HTML5 视频 + 原文 VTT | 读取原字幕、翻译、双语显示；默认不启动 ASR |
| 无字幕英语视频 | 本地识别与真实 API 翻译可形成持续中文字幕 |
| 无字幕日语、韩语视频 | 分别测试；能指定语种并报告质量，不用英语结果代替 |
| 拖动前进、后退、连续拖动 | 没有旧位置字幕回流，不持续处理旧音频 |
| 暂停 30 秒后恢复 | 不重复请求、不漂移、不因后台生命周期失效而卡死 |
| 关闭 Popup、切换标签页 | 会话状态一致；不会意外采集新标签页 |
| 页面内切换视频/SPA 导航 | 媒体身份和缓存正确隔离 |
| 全屏与退出全屏 | 支持的播放器字幕可见，布局正常 |
| 1.5× / 2× 播放 | 有字幕同步正确；ASR 不达标时清楚报告能力限制 |
| 无声、背景音乐、噪声 | 不持续产生虚构字幕或高频翻译请求 |
| 错 Key、错模型、429、超时 | 错误可定位、可恢复，无无限重试 |
| 模型下载失败/被清除 | 给出原因和重新下载入口，不一直显示加载中 |
| iframe/DRM/多播放器 | 按真实能力工作或解释限制，不虚报支持 |
| Windows/macOS | 分别记录实际测过的环境；没条件测的标“未测试” |

除自建测试页外，至少验证两个不同的真实站点，其中至少一个不是 YouTube；注明具体站点、播放器类型、日期、成功/受限原因。不要把通过同一个测试页当作“全网通用”的证据。

## 13. 开发顺序与交付物

### 第一阶段：技术验证

交付最小可运行扩展，验证标签页采集、原音频正常播放、本地多语种 ASR、文字翻译 API、seek 后旧结果隔离。同步写下所用版本、测量结果和限制。

### 第二阶段：完整首版

打通字幕优先/ASR 降级、设置页、模型管理、字幕层、权限引导、故障提示、暂停/seek/全屏及媒体切换。

### 第三阶段：实际使用验证

进行上述关键测试，修复会阻碍观看的问题，整理支持矩阵和安装包。不要为不影响首版的增强功能无限扩大范围。

最终至少交付：

1. 完整项目源码、依赖锁文件和清晰目录结构。
2. 可加载的扩展构建产物；无开发服务器也能启动。
3. README：安装、构建、使用、API 配置、模型下载/删除、隐私、排错。
4. 简短设计说明：关键模块、消息流、媒体时间映射、会话生命周期、seek 处理。
5. 测试记录：实际测过的系统、硬件、模型、网站、延迟和资源占用。
6. 已知限制与未完成项：区别“已经实现”“实测通过”“尚未验证”。

**完成标准：在实际浏览器中，两条流程都能工作——有字幕直接翻译，无字幕真实识别后翻译；用户拖动进度后不会被旧字幕干扰。只有设置页、Mock 字幕或一条成功的 API 请求，不算完成。**

## 14. 给 Claude 的执行要求

- 先检查本地仓库现状和现有约束，保留用户已有工作，再开始开发。
- 按本文默认范围自主推进常规实现选择；不要只输出方案后停止。
- 先验证音频与模型链路，再完善 UI，不要把最大技术风险留到最后。
- 不要求用户把 Key 发到聊天里；通过本地扩展设置输入。
- 需要用户真实环境才能完成的测试，提供最小复现步骤，并如实标为待验证。
- 遇到会改变产品形态、引入音频上传、额外账户/费用或必装本地服务的取舍，先给出测量依据和具体替代方案，再让用户选择。
- 不因希望“快速完成”而把无字幕 ASR、字幕同步或错误处理永久推到后续。
- 不承诺未经测试的站点、语种、速度、跨平台能力或 API 兼容性。

## 15. 实现时应核对的一手资料

以下资料于 2026-09-24 核对，作为实现依据；具体库版本和发布说明仍应在开发时检查。

- [Chrome tabCapture](https://developer.chrome.com/docs/extensions/reference/api/tabCapture)：用户触发、标签页音频、原声音频回放、stream ID 使用范围。
- [Chrome Offscreen API](https://developer.chrome.com/docs/extensions/reference/api/offscreen)：后台文档能力、消息通信、生命周期与可用 API。
- [Chrome 扩展远程托管代码限制](https://developer.chrome.com/docs/extensions/develop/migrate/remote-hosted-code)：JS/WASM 执行代码需要随扩展打包。
- [Transformers.js 官方文档](https://huggingface.co/docs/transformers.js/index)：浏览器模型推理的候选运行时，需验证目标模型、WASM/WebGPU 支持及资源占用。
- [Sub2API 官方网关路由](https://github.com/Wei-Shaw/sub2api/blob/main/backend/internal/server/routes/gateway.go)：确认当前部署版本支持的文字/语音接口，不能仅凭“OpenAI-compatible”判断所有协议通用。

本文中性能目标、队列策略和产品范围属于项目设计要求，不是这些资料对 HearClear 实际性能的保证。
